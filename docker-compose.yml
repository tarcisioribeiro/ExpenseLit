services:
  # Aplicação Streamlit
  app:
    build: .
    ports:
      - "8551:8551"
    volumes:
      - .:/app
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - expenselit_network

volumes: {}

networks:
  expenselit_network:
    driver: bridge 
